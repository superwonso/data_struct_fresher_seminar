#include <stdio.h>

int main()
{
	int T, i;
	scanf("%d", &T);
	while(T--)
	{
		int n, m;
		scanf("%d %d", &n, &m);
		for (i = 0; i < m; i++)
		{
            int a, b; 
            scanf("%d %d", &a, &b);
        }
        printf("%d\n", n-1);
	}
	return 0;
}
